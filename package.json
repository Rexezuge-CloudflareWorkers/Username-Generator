{
    "name": "username-generator",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "predeploy": "npm run buildApp && npm install && npm run cf-typegen",
		"deploy": "wrangler deploy --dry-run",
		"postdeploy": "wrangler deploy",
        "buildApp": "cd app && npm install && npm run release",
        "cf-typegen": "wrangler types"
    },
    "dependencies": {
        "chanfana": "^3.3.0",
        "hono": "^4.12.10",
        "zod": "^4.1.12"
    },
    "devDependencies": {
        "typescript": "^6.0.2",
        "wrangler": "^4.80.0"
    }
}
